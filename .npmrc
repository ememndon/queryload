# QueryLoad — dependency policy
# Non-negotiable rule #7: locked dependency versions (exact, no ^ or ~).
save-exact=true
# Fail loudly rather than silently resolving an engine mismatch.
engine-strict=true
# Reproducible installs; keep the audit surface small and auditable.
fund=false
audit=false
